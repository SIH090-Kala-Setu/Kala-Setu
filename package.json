{
  "name": "artisan-ai",
  "version": "2.0.0",
  "description": "KalaSetu | MoSJE Artisan AI Platform",
  "scripts": {
    "install:backend": "cd backend && python -m venv .venv && .\\.venv\\Scripts\\pip install -r requirements.txt",
    "install:backend:mac": "cd backend && python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "start:backend": "cd backend && .\\.venv\\Scripts\\python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "start:backend:mac": "cd backend && source .venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "start:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "start": "npm run start:frontend",
    "dev": "npm run start:frontend"
  }
}
