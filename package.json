{
  "name": "artisan-ai",
  "version": "1.0.0",
  "description": "Artisan AI Web Platform",
  "scripts": {
    "install:backend": "cd backend && python -m venv .venv && .\\.venv\\Scripts\\pip install -r requirements.txt",
    "install:backend:mac": "cd backend && python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt",
    "start:backend": "cd backend && .\\.venv\\Scripts\\python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "start:backend:mac": "cd backend && source .venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000",
    "start:frontend": "cd frontend && python -m http.server 5500",
    "start:frontend:mac": "cd frontend && python3 -m http.server 5500",
    "start": "npm run start:frontend",
    "dev": "npm run start:frontend"
  }
}
